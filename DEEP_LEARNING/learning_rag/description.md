this one takes a restaurant's reviews in a csv as the knowleadge base and allow for the locally run llama model to use that db, using RAG architecture.

a llm from ollama, as well an embeddings model is required..

this one uses llama3.1 8b llm vs mxbai large embedding model
